---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/components/ui/button.tsx
  - src/components/ui/card.tsx
  - src/lib/utils.ts
  - src/types/critique.ts
  - tailwind.config.ts
  - tsconfig.json
  - package.json
  - next.config.ts
  - .gitignore
autonomous: true

must_haves:
  truths:
    - "Next.js dev server starts with no errors on localhost:3000"
    - "ShadCN Button and Card components render correctly"
    - "TypeScript compiles with no type errors"
    - "Project uses App Router with src/ directory structure"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Root layout with metadata"
    - path: "src/app/page.tsx"
      provides: "Minimal home page placeholder"
    - path: "src/types/critique.ts"
      provides: "TypeScript types for critique structure"
    - path: "src/components/ui/button.tsx"
      provides: "ShadCN Button component"
    - path: "src/components/ui/card.tsx"
      provides: "ShadCN Card component"
---

<objective>
Scaffold the Next.js 15 project with ShadCN/ui, Tailwind CSS, and shared TypeScript types.

Purpose: Establish the foundation that all other phases build on — framework, styling, component library, and type system.
Output: A running Next.js app with ShadCN components and critique type definitions.
</objective>

<execution_context>
@/Users/georgebagaoisan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/georgebagaoisan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 15 project with ShadCN/ui</name>
  <files>
    package.json
    next.config.ts
    tailwind.config.ts
    tsconfig.json
    src/app/layout.tsx
    src/app/page.tsx
    src/app/globals.css
    src/components/ui/button.tsx
    src/components/ui/card.tsx
    src/lib/utils.ts
    .gitignore
  </files>
  <action>
    1. Run `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --use-npm` to scaffold in the current directory. Accept Turbopack if prompted.

    2. Run `npx shadcn@latest init` to initialize ShadCN. Select "New York" style, default theme. The globals.css path will be `src/app/globals.css` due to src directory.

    3. Add initial ShadCN components: `npx shadcn@latest add button card`

    4. Update `src/app/page.tsx` to a minimal placeholder page:
       - Clear the default Next.js boilerplate
       - Show "DesignCritic" heading and a brief "AI-powered design feedback" subtitle
       - Use the ShadCN Card component to wrap content (proves components work)
       - Keep it minimal — no upload UI, no forms, just a styled placeholder

    5. Update `src/app/layout.tsx` metadata:
       - title: "DesignCritic — AI Design Feedback"
       - description: "Get actionable, structured feedback on any UI screenshot instantly"
  </action>
  <verify>
    Run `npm run dev` and confirm the app starts on localhost:3000 with no errors.
    Run `npx tsc --noEmit` to confirm TypeScript compiles cleanly.
  </verify>
  <done>
    Next.js 15 app runs locally with ShadCN Button and Card components rendering on the home page. No TypeScript errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Define shared TypeScript types for critique structure</name>
  <files>
    src/types/critique.ts
  </files>
  <action>
    Create `src/types/critique.ts` with the following types:

    ```typescript
    // Industry contexts for domain-specific heuristics
    export type IndustryContext = 'saas' | 'healthcare' | 'consumer' | 'ecommerce' | 'fintech';

    // Critique tone options
    export type CritiqueTone = 'constructive' | 'roast';

    // A single critique point with summary and expandable detail
    export interface CritiquePoint {
      summary: string;    // 1-2 sentence scannable bullet
      detail: string;     // Deeper explanation with specifics
    }

    // The 4 critique sections
    export interface CritiqueResult {
      what_works: CritiquePoint[];
      usability_risks: CritiquePoint[];
      visual_hierarchy: CritiquePoint[];
      improvements: CritiquePoint[];
    }

    // API request shape
    export interface CritiqueRequest {
      image: string;              // base64-encoded image
      mediaType: 'image/png' | 'image/jpeg' | 'image/webp';
      context: IndustryContext;
      tone: CritiqueTone;
    }

    // API response shape
    export interface CritiqueResponse {
      critique: CritiqueResult;
    }

    // API error response
    export interface CritiqueError {
      error: string;
    }
    ```

    These types will be imported by the API route (Phase 1 Plan 02), prompt engine (Phase 2), and display components (Phase 5). They define the contract between all layers of the app.
  </action>
  <verify>
    Run `npx tsc --noEmit` — types file compiles with no errors.
  </verify>
  <done>
    Critique types are defined in `src/types/critique.ts` covering all 4 sections, request/response shapes, industry contexts, and tone options.
  </done>
</task>

</tasks>

<verification>
1. `npm run dev` starts without errors
2. `npx tsc --noEmit` passes with no type errors
3. localhost:3000 shows the DesignCritic placeholder page with ShadCN styling
4. `src/types/critique.ts` exports all shared types
</verification>

<success_criteria>
- Next.js 15 app runs locally on port 3000
- ShadCN/ui is initialized with Button and Card components available
- TypeScript types for the critique structure are defined and compile cleanly
- Project uses App Router with src/ directory layout
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>

---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - .github/workflows/deploy.yml
autonomous: false

must_haves:
  truths:
    - "Code is pushed to a GitHub repository"
    - "Vercel project is connected to the GitHub repo"
    - "App is live on a Vercel URL with no errors"
    - "ANTHROPIC_API_KEY is set in Vercel environment variables"
  artifacts:
    - path: "GitHub repository"
      provides: "Remote code hosting for CI/CD"
    - path: "Vercel project"
      provides: "Production deployment"
---

<objective>
Push code to GitHub and deploy to Vercel with environment variables configured.

Purpose: Proves the full deployment pipeline works — push to GitHub triggers auto-deploy on Vercel. This satisfies INFR-02 (deploy to Vercel from GitHub with zero-config).
Output: Live app on a Vercel URL, auto-deploying on git push.
</objective>

<execution_context>
@/Users/georgebagaoisan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/georgebagaoisan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub repository and push code</name>
  <files>
    (no new files — git operations only)
  </files>
  <action>
    1. Create a GitHub repository: `gh repo create designcritic --public --source=. --push`
       - This creates the repo, sets origin, and pushes the current branch
       - If `gh` is not authenticated, this becomes a checkpoint:human-action

    2. Verify the push succeeded: `gh repo view --web` (opens in browser)
  </action>
  <verify>
    Run `git remote -v` — origin should point to the GitHub repo.
    Run `gh repo view` — should show the repo details.
  </verify>
  <done>
    Code is pushed to a GitHub repository with all planning files and foundation code.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <action>
    Connect the GitHub repo to Vercel and configure the environment variable:

    1. Go to https://vercel.com/new
    2. Import the `designcritic` GitHub repository
    3. Framework preset will auto-detect "Next.js" — accept defaults
    4. Before clicking Deploy, go to "Environment Variables" section
    5. Add: `ANTHROPIC_API_KEY` = your actual API key from console.anthropic.com
    6. Click Deploy

    Alternatively, if you have the Vercel CLI:
    ```bash
    npm i -g vercel
    vercel --yes
    vercel env add ANTHROPIC_API_KEY production
    ```
  </action>
  <resume-signal>Paste the Vercel deployment URL (e.g., https://designcritic.vercel.app)</resume-signal>
</task>

</tasks>

<verification>
1. GitHub repo exists and contains all project code
2. Vercel deployment is live and accessible
3. The home page loads on the Vercel URL showing the DesignCritic placeholder
4. POST /api/critique on the Vercel URL returns mock critique JSON (proves server-side API key works in production)
</verification>

<success_criteria>
- Code is hosted on GitHub
- App is deployed to Vercel and accessible via public URL
- ANTHROPIC_API_KEY is configured in Vercel environment variables (server-side only)
- Auto-deploy triggers on git push to main
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
